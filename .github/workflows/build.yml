name: Build ZMK Firmware

on:
  push:
    branches:
      - main

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build.yaml@main
    with:
      matrix_path: build.yaml
    secrets: inherit

